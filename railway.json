{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile --ignore-scripts && pnpm --filter @workspace/db run build || true && pnpm --filter @workspace/api-server run build"
  }
}
